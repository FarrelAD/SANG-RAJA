import 'package:flutter/material.dart';

class MasyarakatPage extends StatelessWidget {
  const MasyarakatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Masyarakat screen'),
      ),
    );
  }
}